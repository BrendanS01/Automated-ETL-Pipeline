# Automated-ETL-Pipeline
Automated ETL Pipeline: automates the cleaning process for data, monitors a specific folder for a new csv file, reads the file, removes duplicate and null values,  standardizes the dates, and saves the cleaned version to a new "Processed" folder and archives the original 
